"""경계값 예외처리: 값이 허용 범위를 벗어났을 때 보정하기

반복문 안에서 어떤 변수(포인터, 인덱스 등)를 리셋 없이 계속 이어서 쓰다
보면, 특정 조건에서 그 값이 우리가 기대하는 범위를 벗어나 있는 경우가
생긴다. 이걸 그냥 두고 다음 로직을 실행하면 잘못된 접근/비교로 이어진다.
그래서 로직을 쓰기 전에 "범위를 벗어났으면 최소 허용값으로 끌어올린다"는
예외처리를 먼저 해줘야 한다.

예: ABC466 C - Count Close Pairs (practice/abc466_c_count_close_pairs.py)
    에서 j를 여러 바깥 루프(i)에 걸쳐 리셋 없이 이어서 쓰는데, i가 커지는
    동안 j가 못 따라잡고 뒤처지면 "i < j"라는 문제의 조건이 깨져버린다.
    그래서 매 루프 진입 시 j가 i 이하로 뒤처졌는지 먼저 확인하고
    보정한다.
"""

for i in range(1, n + 1):
    if j <= i:  # 예외처리: j가 i를 못 따라잡았으면 보정
        j = i + 1
    # ... 이후 로직 ...
